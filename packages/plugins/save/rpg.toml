[compilerOptions.build]
  pwaEnabled = false
  outputDir = "./browser"

[vite.build.lib]
    entry = "index.ts"
    name = "RpgSave"
    fileName = "rpg.save"

[vite.build.rollupOptions.output]
  format = "umd"