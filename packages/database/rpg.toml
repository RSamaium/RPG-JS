[compilerOptions.build]
  pwaEnabled = false
  outputDir = "./browser"

[vite.build.lib]
    entry = "src/index.ts"
    name = "RpgDatabase"
    fileName = "rpg.database"

[vite.build.rollupOptions.output]
  format = "umd"