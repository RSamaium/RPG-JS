[compilerOptions.build]
  pwaEnabled = false
  outputDir = "./browser"

[vite.build.lib]
    entry = "src/index.ts"
    name = "RpgServer"
    fileName = "rpg.server"

[vite.build.rollupOptions.output]
  format = "umd"

[vite.build.rollupOptions]
  external = ["@rpgjs/common", "@rpgjs/database"]